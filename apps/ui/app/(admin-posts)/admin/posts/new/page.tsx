import { setMeta } from '@/_libs';

interface Props {}

export const metadata = setMeta({
  title: '',
  url: '',
});

export default function NewPostPage() {
  return (
    <div>content</div>
  );
}
